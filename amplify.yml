version: 1
frontend:
  phases:
    preBuild:
      commands:
        - nvm install 20
        - nvm use 20
        - node --version
        # CRITICAL: Copy PostgreSQL schema BEFORE npm ci
        # This ensures postinstall script uses the correct schema
        - echo "=== Copying PostgreSQL schema for staging ==="
        - cp prisma/schema-postgres.prisma prisma/schema.prisma
        - cat prisma/schema.prisma | head -20
        # Clean any cached Prisma client to force fresh generation
        - rm -rf node_modules/.prisma node_modules/@prisma/client
        - npm ci
        # Verify Prisma client was generated with correct schema
        - echo "=== Verifying Prisma client generation ==="
        - npx prisma generate
        # Verify Prisma binaries were generated
        - echo "Checking for Prisma binaries..."
        - ls -la node_modules/.prisma/client/*.node || echo "Prisma binaries not found!"
        # Run database migrations (PostgreSQL via DATABASE_URL env var)
        - npx prisma migrate deploy
        # Seed historical tournament data (only if SEED_HISTORY env var is set)
        - if [ "$SEED_HISTORY" = "true" ]; then npm run db:seed-history; fi
    build:
      commands:
        - npm run build
        # Create .env.production for runtime
        - echo "=== Creating .env.production for Lambda runtime ==="
        - echo "DATABASE_URL=$DATABASE_URL" > .next/.env.production
        - echo "AUTH_SECRET=$AUTH_SECRET" >> .next/.env.production
        - echo "NEXTAUTH_SECRET=$NEXTAUTH_SECRET" >> .next/.env.production
        - echo "NEXTAUTH_URL=$NEXTAUTH_URL" >> .next/.env.production
        - echo "UPSTASH_REDIS_REST_URL=$UPSTASH_REDIS_REST_URL" >> .next/.env.production
        - echo "UPSTASH_REDIS_REST_TOKEN=$UPSTASH_REDIS_REST_TOKEN" >> .next/.env.production
        - echo "S3_ACCESS_KEY_ID=$S3_ACCESS_KEY_ID" >> .next/.env.production
        - echo "S3_SECRET_ACCESS_KEY=$S3_SECRET_ACCESS_KEY" >> .next/.env.production
        - echo "S3_BUCKET=$S3_BUCKET" >> .next/.env.production
        - echo "S3_REGION=$S3_REGION" >> .next/.env.production
        # Clean up unnecessary files to reduce build artifact size
        - echo "=== Cleaning up cache and unnecessary files ==="
        - rm -rf .next/cache
        - rm -rf node_modules/.cache
        - find .next -name "*.map" -delete
        - echo "=== Build artifact cleanup complete ==="
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
      - .next/cache/**/*
